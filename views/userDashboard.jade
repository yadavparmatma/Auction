extends logged_page_layout
block vars
 - var title = "userDashboard"
 link(rel='stylesheet', href="/stylesheets/style.css")

block content
 div
  center
   h1 Welcome #{userName}
 div
  | List of Auctions you joined
  ul
   each item in itemsDetails
    h2 itemsDetails 
    each val,key in item
     li= key + ': ' + val
